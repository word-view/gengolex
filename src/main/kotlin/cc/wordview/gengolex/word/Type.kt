package cc.wordview.gengolex.word

/**
 * The category of the word. (e.g. noun, verb)
 */
enum class Type {
    /**
     * A word that generally conveys an action.
     */
    VERB,

    /**
     * A word that represents a concrete or abstract thing.
     */
    NOUN
}